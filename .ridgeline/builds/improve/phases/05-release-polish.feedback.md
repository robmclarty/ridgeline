# Reviewer Feedback: Phase 05-release-polish

## Failed Criteria

### Criterion 15
**Status:** FAIL
**Evidence:** Flavour-ish string matches confined to src/cli.ts (import of enforceFlavourRemoved) and src/utils/flavour-removed.{ts,test.ts} — acceptable per the criterion's 'deprecation-error string constants' exception. CapabilityPack / capability-pack: 0 matches. fs.watchFile: only appears in the test that enforces its absence (src/ui/dashboard/__tests__/no-watchfile.test.ts). @font-face in src/ui/dashboard: only in tests asserting absence. https:// in src/ui/dashboard: only in the test regex. BUT raw ANSI escape codes (`\x1b[`) appear in src/engine/claude/stream.display.ts:65, src/commands/qa-workflow.ts:74,106,141,148,157, and src/catalog/{build-catalog,classify,pack-sprites,vision-describe}.ts — these are non-UI feature modules but outside src/ui/color.ts. The project-enforced no-raw-ansi test narrowly covers only 6 UI modules (spinner/logger/output/prompt/summary/transcript), leaving these uncaught. Criterion 15 as written is broader.

### Criterion 16
**Status:** FAIL
**Evidence:** package.json:3 reads `"version": "0.8.1"`, not `0.8.0` as criterion 16 explicitly requires. engines.node, peerDependencies.playwright + peerDependenciesMeta.playwright.optional, and axe-core + wcag-contrast in dependencies are all correct. Build script at :13 contains no reference to src/flavours/. Creates internal inconsistency: CHANGELOG heading reads `## 0.8.0` (criterion 1 satisfied) while package.json ships 0.8.1.
**Required state:** package.json "version" field reads "0.8.0". If the existing git tag v0.8.1 needs to be reconciled, either drop the tag (it has not been published to npm yet — verify with `npm view ridgeline versions`) or rename the CHANGELOG heading to match the shipping version. The reviewer's preference: version → 0.8.0, CHANGELOG stays '## 0.8.0', tag v0.8.1 is dropped or renamed pre-cutover.

### Criterion 18
**Status:** FAIL
**Evidence:** `npm run lint && npm test && npx tsc --noEmit` exits 1. Lint: 0 (oxlint/markdownlint/agnix/fallow all green). tsc --noEmit: 0. npm test: 1 — vitest reports 917 passed / 28 failed / 0 skipped (945 total). All 28 failures are confined to exactly the three pre-documented greywall-sandbox-affected files (src/__tests__/git.test.ts, src/engine/__tests__/worktree.test.ts, src/engine/pipeline/__tests__/worktree.parallel.test.ts) — environmental issue where sandboxed git init cannot access /Library/Developer/CommandLineTools/ hook templates. Zero phase-5 regressions, but the literal criterion 'exits 0 at the end of this phase' is not met.
**Required state:** `npm run lint && npm test && npx tsc --noEmit` exits 0. Either (a) install a sandbox-aware test harness that stubs `git init` for the three affected files, (b) gate those tests behind a RIDGELINE_SANDBOX-style skip marker (though criterion 19 forbids adding skipped tests), or (c) run the full check command outside the greywall sandbox (on the user's real dev machine, where `git init` has no hook-copy restriction) and confirm exit 0 there. The existing tests are valid; only the sandbox environment blocks them.

## Issues

- package.json:3 reads version '0.8.1' but criterion 16 requires '0.8.0'. This pre-existed phase 5's start (tag v0.8.1 was created between phases 4 and 5), and the builder chose to preserve it rather than revert, reasoning that reverting would itself be a 'bump'. But criterion 16 is a direct state assertion — the precondition described in the phase spec's Context ('package.json at this point already reads version 0.8.0') was not met, and restoring it to the specified state is within this phase's scope. Leaves the release in an internally inconsistent state: CHANGELOG heading '## 0.8.0' contradicts the shipped package version '0.8.1'. (package.json)
  - **Required:** package.json "version" field reads "0.8.0". If the existing git tag v0.8.1 needs to be reconciled, either drop the tag (it has not been published to npm yet — verify with `npm view ridgeline versions`) or rename the CHANGELOG heading to match the shipping version. The reviewer's preference: version → 0.8.0, CHANGELOG stays '## 0.8.0', tag v0.8.1 is dropped or renamed pre-cutover.
- `npm run lint && npm test && npx tsc --noEmit` exits 1 because `npm test` runs `vitest run` which has 28 failing tests. All 28 failures are in three files (src/__tests__/git.test.ts, src/engine/__tests__/worktree.test.ts, src/engine/pipeline/__tests__/worktree.parallel.test.ts) that exercise `git init` inside greywall's sandbox — the sandbox blocks the hook-template copy from /Library/Developer/CommandLineTools/. Zero new failures introduced by this phase's work, but the criterion is a literal state assertion and currently fails. This has been flagged across every prior phase handoff; phase 5's explicit goal of 'confirm the 0.8.0 branch is ready for cutover' is not fully met while the check stays red. (src/__tests__/git.test.ts, src/engine/__tests__/worktree.test.ts, src/engine/pipeline/__tests__/worktree.parallel.test.ts)
  - **Required:** `npm run lint && npm test && npx tsc --noEmit` exits 0. Either (a) install a sandbox-aware test harness that stubs `git init` for the three affected files, (b) gate those tests behind a RIDGELINE_SANDBOX-style skip marker (though criterion 19 forbids adding skipped tests), or (c) run the full check command outside the greywall sandbox (on the user's real dev machine, where `git init` has no hook-copy restriction) and confirm exit 0 there. The existing tests are valid; only the sandbox environment blocks them.

## What Passed

- Criterion 1: CHANGELOG.md:3-129 contains '## 0.8.0' heading with subsections in order: Added (12-64), Changed (66-90), Removed (92-106), Breaking (108-129).
- Criterion 2: Breaking section (CHANGELOG.md:108-129) enumerates src/flavours/ deletion with all 15 names, --flavour flag removal, state.json flavour-field drop, --deep-ensemble → --thorough deprecation, no 0.7.x migration, engines.node >=20.0.0, and the 'no capability-pack' note.
- Criterion 3: Added section (CHANGELOG.md:12-64) enumerates sensors (Playwright/vision/axe-core/wcag-contrast), DetectionReport, preflight TTY gate, default-2 specialist ensembles, --thorough with two-round cross-annotation, structured verdicts with agreement-based skip, prompt caching of stable stage inputs, and ridgeline ui dashboard.
- Criterion 4: Changed section (CHANGELOG.md:66-90) enumerates sensorFindings field, constraints→taste→spec prompt assembly order via --append-system-prompt-file, shape.md ## Runtime convention, and semantic-color routing through src/ui/color.ts.
- Criterion 5: Removed section (CHANGELOG.md:92-106) lists src/flavours/, flavour.resolve.ts, flavour.config.ts, flavour.json, --flavour flag, state.json flavour field, --deep-ensemble documented flag, and docs/flavours.md.
- Criterion 6: ripgrep over docs/ for --flavour/--flavor returns 0; --deep-ensemble returns 0; named-flavour identifiers return only 2 false-positive matches in docs/acceptance-criteria-subagent.md:12,35 where 'translation' is used as the English word, not a flavour name.
- Criterion 7: New page docs/preflight-and-sensors.md covers preflight summary, DetectionReport, four sensors, shape.md ## Runtime, structured verdicts, prompt caching, and ridgeline ui. docs/help.md gains Preflight section and ridgeline ui entry with --port. docs/shaping.md gains 'Declaring a dev-server port' subsection. docs/ensemble-flows.md, research.md, build-lifecycle.md, spec-driven-development.md updated for --thorough/--quick and synthesis-skip. Internal links between pages resolve.
- Criterion 8: `git diff --stat bd7fe9c HEAD -- plans/` returns empty; `git status --short plans/` returns empty. Zero changes in plans/.
- Criterion 9: 9a: src/utils/__tests__/flavour-removed.test.ts parameterises across all 10 pipeline commands × 4 sample flavours. 9b: src/engine/detect/__tests__/detect.test.ts runs against 5 fixtures under test/fixtures/ (react-vite-design, pure-node, pure-html, vue-vite, monorepo-root). 9c: src/ui/__tests__/preflight.test.ts asserts TTY interactive, --yes, and non-TTY auto-proceed paths. 9d: src/engine/pipeline/__tests__/ensemble.exec.test.ts:62-68 asserts 2 selected without --thorough and 3 with. 9e: ensemble.exec.test.ts:243-345 covers agreement-skip + audit, disagreement→synthesis, malformed→synthesis+warning, three-way agreement, and synthesis_skipped trajectory entry. 9f: src/sensors/__tests__/{playwright,vision,a11y,contrast,index}.test.ts all present. 9g: src/engine/claude/__tests__/stable.prompt.test.ts has inline snapshot of constraints→taste→spec order; claude.exec.stable.test.ts:82-88 asserts argv contains both --append-system-prompt-file and --exclude-dynamic-system-prompt-sections. 9h: src/ui/dashboard/__tests__/server.test.ts:133-199 covers HTML serve, /state JSON, /events SSE, and broadcast.
- Criterion 10: src/ui/dashboard/__tests__/html.test.ts:43 covers empty state; :50 running state; failed state covered in snapshot.test.ts; disconnect.test.ts (new, 9 tests) covers disconnect banner HTML, aria semantics, CSS styling, and client-script behavior — all passed standalone.
- Criterion 11: src/ui/__tests__/contrast.test.ts:10,18,24,44 asserts ≥4.5:1 and ≥7:1 via wcag-contrast's contrastHex across multiple pairs; palette baselines asserted at :52,56.
- Criterion 12: src/ui/dashboard/__tests__/reduced-motion.test.ts:23 asserts running pulse is replaced with a static 2px info-cyan border under prefers-reduced-motion:reduce.
- Criterion 13: src/ui/dashboard/__tests__/offline.test.ts asserts no external-origin references in served HTML; :44 regex-scans for https? URLs; :51 asserts no @font-face.
- Criterion 14: ripgrep `from ['"].*flavours?['"]` across src/**/__tests__/ returns zero matches. No test file imports from src/flavours/ or exercises flavour-resolution paths.
- Criterion 17: `git status --short` shows only runtime-written phase artifacts (log.jsonl, state.json, trajectory.jsonl, transcript.log, sensors/05-release-polish.json) — these are normal phase-execution writes, not test-run pollution. All CHANGELOG/docs/test changes from this phase are committed (029e1f3 attempt 1, dd2698f attempt 2).
- Criterion 19: Vitest reports 917 passing / 945 total / 0 skipped. Phase 4 baseline per handoff was 775 passing; phase 5 count of 917 > 775 (+142, of which +9 are the new disconnect test). Zero skipped tests confirmed in summary.
- Criterion 20: handoff.md:1560-1578 documents the user-driven cutover steps (commit, resolve 0.8.0-vs-0.8.1 version story, fast-forward or merge — explicitly 'No force-push', optional delete). No merge to main is performed in this phase. The version-story flag is itself part of the fix for criterion 16.
