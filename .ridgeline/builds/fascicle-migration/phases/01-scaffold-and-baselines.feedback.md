# Reviewer Feedback: Phase 01-scaffold-and-baselines

## Failed Criteria

### Criterion 2
**Status:** FAIL
**Evidence:** package.json:46 declares `zod: ^4.1.8`, not `^3.` as required. Builder documented this as a deviation in baseline/capability-matrix.md and handoff.md. Root cause: node_modules/fascicle/package.json peerDeps require `zod ^4.0.0`, so spec criterion conflicts with substrate runtime.
**Required state:** Either (a) the spec is amended (in spec.md and constraints.md) to allow zod `^4.x` to match fascicle's peer dep, with the change recorded in the next CHANGELOG entry; or (b) zod is pinned at `^3.x` and fascicle is downgraded/swapped to a version compatible with zod 3 (none of fascicle's 0.3.x line satisfies this, so option (a) is the realistic path).

### Criterion 12
**Status:** FAIL
**Evidence:** baseline/mutation-score.json has `score: null` and `captured: false`. Stryker's worker IPC was blocked by the greywall sandbox (EPERM AggregateError on internalConnectMultiple, Node v24.15.0 + Stryker 9.6.1). The file is a documentation-only placeholder. The criterion requires the score to be recorded at this commit.
**Required state:** Either (a) `score`, `killed`, `survived`, `timeout`, `no_coverage`, `compile_errors`, `runtime_errors`, `ignored` populated with real Stryker output for src/engine/pipeline/**/*.ts and `captured: true`; or (b) explicit human approval recorded in handoff.md to defer baseline capture to Phase 7, with the spec criterion #12 amended accordingly.

### Criterion 23
**Status:** FAIL
**Evidence:** HEAD commit subject is `ridgeline: builder work for 01-scaffold-and-baselines (attempt 3)`, generated by the migration harness's builder. The criterion requires the phase exit commit subject to begin with `phase-0:`.
**Required state:** A new commit (or amended HEAD if no other authors are involved) whose subject begins with `phase-0:` followed by a short imperative summary (e.g. `phase-0: scaffold dependencies and capture pre-migration baselines`).

## Issues

- baseline/mutation-score.json records `score: null` and `captured: false` — Stryker's worker IPC was blocked by the greywall sandbox (EPERM AggregateError on internalConnectMultiple). The file is a documented placeholder, not a captured baseline. Per the reviewer rubric, this is a tool-failure fallback that warrants halting Phase 0 so the human can decide whether to (a) re-run Stryker outside the sandbox now and capture the real number, (b) amend the spec to defer the baseline to Phase 7, or (c) accept the placeholder. The handoff itself flags this as `_blocking_for_phase_8: true` — surfacing it now matches that intent. (.ridgeline/builds/fascicle-migration/baseline/mutation-score.json)
  - **Required:** Either (a) `score`, `killed`, `survived`, `timeout`, `no_coverage`, `compile_errors`, `runtime_errors`, `ignored` populated with real Stryker output for src/engine/pipeline/**/*.ts and `captured: true`; or (b) explicit human approval recorded in handoff.md to defer baseline capture to Phase 7, with the spec criterion #12 amended accordingly.
- package.json declares `zod: ^4.1.8`, but the acceptance criterion mandates `^3.`. The deviation has a defensible reason — fascicle 0.3.8 peer-deps require `zod ^4.0.0`, so satisfying the criterion strictly would break the substrate — but the criterion as written is unambiguously not met. This is a spec/substrate conflict the orchestrator should resolve before Phase 1 imports start consuming zod. (package.json)
  - **Required:** Either (a) the spec is amended (in spec.md and constraints.md) to allow zod `^4.x` to match fascicle's peer dep, with the change recorded in the next CHANGELOG entry; or (b) zod is pinned at `^3.x` and fascicle is downgraded/swapped to a version compatible with zod 3 (none of fascicle's 0.3.x line satisfies this, so option (a) is the realistic path).
- HEAD commit subject is `ridgeline: builder work for 01-scaffold-and-baselines (attempt 3)`, generated by the migration harness's builder loop. The acceptance criterion explicitly requires the phase exit commit subject to begin with `phase-0:`. As-is, downstream automation that filters phase-exit commits by subject prefix will not match this commit. (git HEAD (commit 0a0e3af))
  - **Required:** A new commit (or amended HEAD if no other authors are involved) whose subject begins with `phase-0:` followed by a short imperative summary (e.g. `phase-0: scaffold dependencies and capture pre-migration baselines`).

## What Passed

- Criterion 1: package.json:42 declares `fascicle: ^0.3.8` under dependencies.
- Criterion 3: package.json:10 sets `engines.node` to `>=24.0.0`.
- Criterion 4: package.json contains zero matches for `@ai-sdk/anthropic` or `"ai":` in dependencies, devDependencies, or peerDependencies (verified by grep -c).
- Criterion 5: No .github/workflows/ directory exists in the repo, so the criterion is vacuously satisfied. Handoff notes any future CI must use Node 24 only.
- Criterion 6: src/engine/{flows,atoms,composites,adapters}/index.ts each exist and contain only `export {}`.
- Criterion 7: 22 baseline/help/<command>.txt files exist covering every command in the criterion's minimum list. The four fall-through commands (auto, create, input, qa-workflow) capture the default help output verbatim — README.md documents this and notes Phase 5 promotion would force an explicit refresh.
- Criterion 8: 22 baseline/dts/<command>.d.ts files exist, one per src/commands/*.ts (plus index.d.ts), produced via `tsc --emitDeclarationOnly` per README.md.
- Criterion 9: baseline/fixtures/ contains trajectory.jsonl, state.json, budget.json, and phases/01-hello-script.md from a recorded successful build.
- Criterion 10: baseline/fixtures/error-shapes.json records error.name, message, and trajectory event kind for adversarial round-cap exhaustion, schema-validation failure, auth failure, and budget-exceeded paths.
- Criterion 11: baseline/fixtures/builder-modelcall-input.json records the resolved system+user-prompt+tools+sandbox payload for a frozen BuilderArgs input. Note: `fingerprint.sha256_hex` is left null with rationale that Phase 5 will materialize it from the new substrate to avoid locking against pre-migration stringification — acceptable interpretation of 'records the ModelCallInput'.
- Criterion 13: baseline/capability-matrix.md verifies fascicle 0.3.8's claude_cli capabilities against node_modules/fascicle/dist/{index.d.ts,index.js} — sandbox kinds (bwrap/greywall, none=undefined), auth modes, StreamChunk variants, CostBreakdown shape, AbortSignal propagation, model alias set, startup_timeout_ms=120_000, stall_timeout_ms=300_000, install_signal_handlers default=true. Honestly flags `skip_probe` as declared-but-unobserved and `fascicle/adapters` subpath as not exported in 0.3.8 — both gaps for later phases to confirm.
- Criterion 14: baseline/exit-codes.md enumerates every exit code (0, 1, 130) with trigger conditions, source paths, and the SIGINT teardown sequence pinned for Phase 6 invariant 5.
- Criterion 15: baseline/greywall-tests.txt enumerates every describe/it block under src/engine/claude/__tests__/sandbox.greywall.test.ts and sandbox.test.ts, plus references in claude.exec.test.ts and pipeline.shared.test.ts.
- Criterion 16: baseline/sandbox-allowlist.{semi-locked,strict}.json record the 16-host network allowlist derived from src/stores/settings.ts. Both files have identical host sets because pre-migration ridgeline doesn't vary the allowlist by mode — README.md and the strict file's _strict_profile_callout explicitly document this so Phase 3 can decide whether to narrow strict's host set.
- Criterion 17: baseline/README.md documents provenance (commit, capture host, tool versions, terminal width), per-artifact reproduction commands, and a regeneration checklist.
- Criterion 18: CHANGELOG.md:3-33 contains the new `Unreleased — v0.12.0` entry with: (a) prominent BREAKING-FOR-CONSUMERS callout for the Node 24 bump, (b) internal substrate migration bullet, (c) public-CLI-unchanged bullet.
- Criterion 19: Five new ast-grep rules under rules/ are staged at `severity: hint` with comments noting their activation phase: no-create-engine-outside-factory (Phase 6), no-console-in-engine-substrate (Phase 4), no-pipeline-imports-in-engine-substrate (Phase 4-5), no-fascicle-alias-reexports (Phase 5), no-emoji-in-engine-substrate (Phase 4). `ast-grep scan` exits 0.
- Criterion 20: Verifier-confirmed `npm run check` exit 0, all 8 sub-checks pass (types, lint, struct, agents, dead, docs, spell, test) in ~12.2s. Re-run reproduces the result; cached .check/summary.json matches.
- Criterion 21: The build that produced this commit ran ridgeline build end-to-end on the migration build directory (with two transient timeouts before attempt 3 succeeded — see log.jsonl/transcript.log). dist/cli.js builds and `--help` prints byte-equal to baseline/help/ridgeline.txt.
- Criterion 22: .ridgeline/builds/fascicle-migration/phase-0-check.json captures the .check/summary.json snapshot at the exit commit with `ok: true` and zero failures across all 8 checks.
