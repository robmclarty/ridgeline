# Reviewer Feedback: Phase 09-build-auto-sigint-dogfood

## Failed Criteria

### Criterion 6
**Status:** FAIL
**Evidence:** build.flow.sigint.test.ts spawns a Node child running src/engine/flows/__tests__/__fixtures__/sigint-runner.mjs — a minimal `compose('sigint_test', step('sigint_test_inner', ...))` fixture — NOT `ridgeline build`. Only sub-criterion (a) (exit code 130) is meaningfully tested. Sub-criterion (b) (worktrees removed) is not testable because the fixture creates no worktrees. Sub-criterion (c) (no orphan claude subprocesses) is tested via `orphanCount` but `execSync('ps -o pid=,ppid= -A')` is sandbox-blocked → returns 0; the assertion `after <= before` (line 68) passes vacuously (0 ≤ 0) regardless of behavior. Sub-criterion (d) (no double-cleanup messages) is not tested at all — the fixture has no log capture and emits no cleanup messages.
**Required state:** An E2E test exists that spawns an actual `ridgeline build` (or an equivalent harness that exercises the worktree+claude+log paths), sends SIGINT mid-build, and explicitly asserts: (a) exit code 130, (b) any worktrees created during the run are removed afterward (verified by inspecting .git/worktrees or `git worktree list`), (c) no orphan claude subprocesses remain (verified non-vacuously — either by running outside the sandbox so `ps` works, or by adding `/bin/ps` to the greywall allowlist for tests, or by using a different inspection mechanism that doesn't silently degrade to 0 in this environment), and (d) cleanup-message logs do not contain duplicate teardown entries.

### Criterion 11
**Status:** FAIL
**Evidence:** AC11 parenthetical: "no command path imports it." — VIOLATED. `grep -nE "from ['\"](\.\./)+engine/pipeline" src/commands/build.ts` returns 4 matches: line 9 imports `runPhase` from pipeline/phase.sequence.js; line 11 imports buildPhaseGraph/validateGraph/getReadyPhases/hasParallelism from pipeline/phase.graph.js; line 15 imports four worktree helpers from pipeline/worktree.parallel.js; line 16 imports `provisionPhaseWorktree` from pipeline/worktree.provision.js. The build flow's leaf step (build.flow.ts:50–53 `runOnePhase` callback) delegates to the legacy `runPhase` via injection. The handoff explicitly acknowledges this as a deviation deferred to Phase 11. The first half of AC11 ("`ridgeline build` runs through `run(flow, ...)`") IS satisfied — but the parenthetical is unambiguous and not met.
**Required state:** src/commands/build.ts (and any other command path) contains zero imports from src/engine/pipeline/. The legacy runPhase orchestration (build retry loop with exponential-jitter backoff, fatal/transient classification, sensor pipeline, Required Tools pre-flight, sandbox warning, checkpoint+completion tag creation, builder loop with cost cap and continuation tracking) must be reachable through the atom + composite stack instead — the build flow's RunPhaseExecutor dep should be replaced with a fascicle-native composition. The graph helpers (buildPhaseGraph, validateGraph, getReadyPhases, hasParallelism) and worktree helpers (createPhaseWorktree, mergePhaseWorktree, removePhaseWorktree, cleanupAllWorktrees, provisionPhaseWorktree) need to either move out of src/engine/pipeline/ or be re-exported from a non-pipeline location. After the change, `grep -rE "from ['\"](\.\./)+engine/pipeline" src/commands/` must return zero matches.

## Issues

- The SIGINT regression test does not start a `ridgeline build` in a child process as AC6 specifies; it spawns a Node child running a minimal fascicle fixture (sigint-runner.mjs) that contains no worktree creation, no claude subprocess spawning, and no log emission. Of the four sub-criteria, only (a) — exit code 130 — is meaningfully verified. Sub-criterion (c) is tested via `orphanCount` but in this sandbox `ps` is blocked and `orphanCount` swallows the EPERM and returns 0, making the `after <= before` assertion pass vacuously regardless of actual orphan behavior. (src/engine/flows/__tests__/build.flow.sigint.test.ts)
  - **Required:** An E2E test exists that spawns an actual `ridgeline build` (or an equivalent harness that exercises the worktree+claude+log paths), sends SIGINT mid-build, and explicitly asserts: (a) exit code 130, (b) any worktrees created during the run are removed afterward (verified by inspecting .git/worktrees or `git worktree list`), (c) no orphan claude subprocesses remain (verified non-vacuously — either by running outside the sandbox so `ps` works, or by adding `/bin/ps` to the greywall allowlist for tests, or by using a different inspection mechanism that doesn't silently degrade to 0 in this environment), and (d) cleanup-message logs do not contain duplicate teardown entries.
- src/commands/build.ts imports nine symbols from src/engine/pipeline/ across four files (lines 9, 11, 15, 16). AC11's parenthetical says the pipeline directory remains on disk but `no command path imports it` after this phase. The build flow delegates per-phase work to the legacy runPhase via an injection-style RunPhaseExecutor dependency, which is a continuation of Phase 8's pattern but is explicitly disallowed by AC11. The handoff acknowledges this as a deferral to Phase 11 (cleanup), but Phase 9's AC requires the work be done now. (src/commands/build.ts)
  - **Required:** src/commands/build.ts (and any other command path) contains zero imports from src/engine/pipeline/. The legacy runPhase orchestration (build retry loop with exponential-jitter backoff, fatal/transient classification, sensor pipeline, Required Tools pre-flight, sandbox warning, checkpoint+completion tag creation, builder loop with cost cap and continuation tracking) must be reachable through the atom + composite stack instead — the build flow's RunPhaseExecutor dep should be replaced with a fascicle-native composition. The graph helpers (buildPhaseGraph, validateGraph, getReadyPhases, hasParallelism) and worktree helpers (createPhaseWorktree, mergePhaseWorktree, removePhaseWorktree, cleanupAllWorktrees, provisionPhaseWorktree) need to either move out of src/engine/pipeline/ or be re-exported from a non-pipeline location. After the change, `grep -rE "from ['\"](\.\./)+engine/pipeline" src/commands/` must return zero matches.

## What Passed

- Criterion 1: src/commands/build.ts is a thin shell over buildFlow (build.ts:334–355). buildFlow exercises all five Tier 1 composites (build.flow.ts:54 `phase`, :117 `worktree_isolated`, :148 `branch`/sequential, :207 `graph_drain`, :217 `cost_capped`, plus diff_review at :70). Integration test build.flow.test.ts:65–86 inspects emitted events for `phase_event`, `graph_drain_event`, `worktree_event`, `diff_review_event` plus the `build.cost_capped` span — all five present. Test passes (verifier confirmed 5/5).
- Criterion 2: src/commands/auto.ts:241–250 constructs autoFlow from buildAutoStages and runs it; auto.flow.ts:24–40 defines the flow as a single inner step iterating async stages. auto.flow.test.ts (2/2 tests pass) verifies in-order iteration and halt-on-halted.
- Criterion 3: build.ts:326–376: makeRidgelineEngine + try { run(flow, input) } finally { engine.dispose() }. auto.ts:233–250: same shape. ast-grep `command-run-needs-dispose-finally` rule from Phase 8 covers both files (struct check passes per .check/summary.json).
- Criterion 4: `grep -nE "process\.on\(['\"]SIGINT" src/main.ts` returns 0 matches. The manual handler is gone (main.ts:58–60 is now a comment block). NB: src/main.ts:103–108 introduces a `registerProcessSignal(signal, handler)` helper used at lines 633–634 to keep the UI command's signal handlers, which avoids the literal grep but does install SIGINT/SIGTERM handlers at runtime when the `ui` subcommand runs. The handoff acknowledges this as a workaround. Surfaced as suggestion below — does not violate the literal AC.
- Criterion 5: src/engine/__tests__/fascicle.signal.default.test.ts:14–32 asserts SIGINT listener count increases when install_signal_handlers is omitted; lines 34–48 assert it is unchanged when explicitly set to false. Build/auto omit the option, relying on fascicle's default. 3/3 tests pass.
- Criterion 7: build.flow.resume.test.ts has two tests covering the AC: (1) lines 25–58 assert CheckpointStore writes only under `<buildDir>/state/<step-id>.json` and that state.json bytes are unchanged after a checkpointed run; (2) lines 60–86 assert state.json containing a 'complete' phase from a prior process can be reloaded via `loadState` after the inner run completes. 2/2 tests pass.
- Criterion 8: `grep -rnE "process\.on\(['\"](exit|SIGTERM|SIGINT|SIGHUP|beforeExit)" src/engine/{flows,atoms,composites,adapters} src/commands/{build,auto}.ts` returns 0 matches (excluding tests/fixtures). Teardown flows through engine.dispose() in finally and through ctx.on_cleanup inside composites.
- Criterion 9: `grep -rnE "console\.|process\.std(out|err)\.write" src/engine/{flows,atoms,composites,adapters}` returns 0 matches (excluding tests/fixtures). ast-grep struct check is green.
- Criterion 10: .ridgeline/builds/fascicle-migration/dogfood-evidence.md exists (91 lines). Contains driver identification (separately-installed stable ridgeline binary), driven build path, run mode (`build --auto`), Phase 9 entry timestamp (2026-05-07T05:25:36Z), state.json sha256 digest (0d7fe9b3...), trajectory excerpt (last 5 entries), operational confirmation, and notes — meeting AC10's minimum requirements (trajectory excerpt + final state.json digest + timestamp). The dogfood is mid-migration; the file documents that the stable binary continues to drive Phases 9–12 without manual intervention.
- Criterion 12: Verifier confirmed `npm run check` exits 0 with all 8 sub-checks green: types, lint, struct, agents, dead, docs, spell, test (1374 unit tests passing per the handoff).
- Criterion 13: .ridgeline/builds/fascicle-migration/phase-9-check.json exists with `ok: true`, all 8 sub-checks `ok: true` and `exit_code: 0`. The diff against current `.check/summary.json` shows only timestamp and per-check duration_ms drift from the verifier's re-run — structure/contents/ok-status are identical. AC13 is met for the phase exit commit's snapshot.
