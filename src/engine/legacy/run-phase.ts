// Provisional bridge to the legacy runPhase orchestration. Phase 11 cleanup
// replaces this with the atom + composite stack and deletes the file.
export { runPhase } from "../pipeline/phase.sequence.js"
