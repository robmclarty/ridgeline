// Provisional bridge to the legacy research executor. Phase 11 cleanup replaces
// this with the atom + composite stack and deletes the file.
export { invokeResearcher, type ResearchConfig } from "../pipeline/research.exec.js"
