// Provisional bridge to the legacy specifier executor. Phase 11 cleanup
// replaces this with the atom + composite stack and deletes the file.
export { invokeSpecifier, type SpecEnsembleConfig } from "../pipeline/specify.exec.js"
