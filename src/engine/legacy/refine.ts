// Provisional bridge to the legacy refine executor. Phase 11 cleanup replaces
// this with the atom + composite stack and deletes the file.
export { invokeRefiner, type RefineConfig } from "../pipeline/refine.exec.js"
